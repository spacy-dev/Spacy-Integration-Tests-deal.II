sudo: required

language: c++

services:
  - docker

install:
  - DEALII_TEST_DIR="/home/travis"
  - docker pull lubkoll/boost-dealii:latest

script:
  - docker run --rm -ti -v ${TRAVIS_BUILD_DIR}:${DEALII_TEST_DIR} lubkoll/boost-dealii:latest ${DEALII_TEST_DIR}/run_test_in_docker.sh

